before_script: 
  - source /etc/profile.d/rvm.sh
  - rvm use 2.2@mine
  #- gem install bundler
  - bundle install 
  #- bundle exec rake db:create

rspec: 
  script: "rake spec"
  tags: 
    - ruby
    - postgres
  only: 
    - branches

#staging: 
#  script: "cap deploy stating"
#  type: deploy
#  tags: 
#    - capistrano
#    - debian
#  except:
#    - stable
#    - /^deploy-.*$/
